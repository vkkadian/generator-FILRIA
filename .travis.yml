language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-FILRIA' ]; then cd .. && eval "mv $currentfolder generator-FILRIA" && cd generator-FILRIA; fi

